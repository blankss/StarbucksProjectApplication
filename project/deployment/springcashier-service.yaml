apiVersion: v1
kind: Service
metadata:
  name: spring-cashier-service
  namespace: default
spec:
  type: ClusterIP
  ports:
    - port: 90
      targetPort: 5000
    - port: 1956
      targetPort: 1956
  selector:
    name: spring-cashier
